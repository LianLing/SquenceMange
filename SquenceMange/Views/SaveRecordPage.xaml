<Page x:Class="SquenceMange.Views.SaveRecordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      d:DesignHeight="950" d:DesignWidth="800"
      Title="标签管理">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 搜索栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBox x:Name="searchBox" 
                     Width="250" Height="28" Margin="10,0"
                     TextWrapping="Wrap"
                     TextChanged="SearchBox_TextChanged"
                     Tag="输入料号或创建人"/>

            
        </StackPanel>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="400,3,0,12">
            <Button Content="查询" Width="60" Margin="5" Click="SearchButton_Click"/>
            <Button Content="新增" Width="60" Margin="5" Click="AddButton_Click"/>
            <Button Content="删除" Width="60" Margin="5" Click="DeleteButton_Click"/>
            <Button Content="修改" Width="60" Margin="5" Click="EditButton_Click"/>
            <Button Content="保存" Width="60" Margin="5" Click="SaveButton_Click"/>
        </StackPanel>

        <!-- 数据表格 -->
        <DataGrid Grid.Row="1" 
                  x:Name="tagsDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  ItemsSource="{Binding TagsList}"
                  SelectedItem="{Binding TagsListSingle}">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="60"/>
                <DataGridTextColumn Header="料号" Binding="{Binding MaterialId}" IsReadOnly="True" Width="120"/>
                <DataGridTextColumn Header="图纸" Binding="{Binding PictureAddress}" Width="60"/>
                <DataGridTextColumn Header="模板" Binding="{Binding ModelName}" Width="60"/>
                <DataGridCheckBoxColumn Header="已生成序列号" Binding="{Binding IsCreated}" Width="90"/>
                <DataGridTextColumn Header="打印次数" Binding="{Binding PrintTimes}" Width="70"/>
                <DataGridTextColumn Header="关联机型" Binding="{Binding ConnectMachine}" Width="70"/>
                <DataGridTextColumn Header="创建人" Binding="{Binding Creater}" Width="100"/>
                <DataGridTextColumn Header="创建时间" 
                   Binding="{Binding CreateTime, StringFormat=yyyy-MM-dd}" 
                   Width="120"/>
                <DataGridTextColumn Header="修改人" Binding="{Binding Editor}" Width="100"/>
                <DataGridTextColumn Header="修改时间" 
                                  Binding="{Binding EditTime, StringFormat=yyyy-MM-dd}" 
                                  Width="120"/>
                <DataGridTextColumn Header="备注" Binding="{Binding Remark}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>