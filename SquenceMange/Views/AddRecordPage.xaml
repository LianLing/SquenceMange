<Page x:Class="SquenceMange.Views.AddRecordPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="clr-namespace:SquenceMange.ViewModels"
      Title="新增标签" Height="600" Width="500">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="新建标签记录" 
                  FontSize="20" FontWeight="Bold"
                  Margin="0,0,0,20"/>

        <!-- 输入表单 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- MaterialId -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="料号：" Width="80" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtMaterialId" MaxLength="200" 
                           Width="300" Tag="必填项"/>
                </StackPanel>

                <!-- PictureAddress -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="图纸路径：" Width="80" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtPicture" MaxLength="200" Width="300"/>
                </StackPanel>

                <!-- ModelName -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="模板名称：" Width="80" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtModel" MaxLength="50" Width="300"/>
                </StackPanel>

                <!-- SequenceId -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="序列号：" Width="80" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtSequenceId" MaxLength="50" Width="300"/>
                </StackPanel>
                
                <!-- ConnectMachine -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="关联机型：" Width="80" VerticalAlignment="Center"/>
                    <TextBox x:Name="txtMachine" MaxLength="100" Width="300"/>
                </StackPanel>

                <!-- Remark -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <Label Content="备注：" Width="80" VerticalAlignment="Top"/>
                    <TextBox x:Name="txtRemark" MaxLength="255" 
                           Height="80" Width="300"
                           AcceptsReturn="True" 
                           VerticalScrollBarVisibility="Auto"/>
                </StackPanel>

                <!-- 操作按钮 -->
                <StackPanel Orientation="Horizontal" 
                          HorizontalAlignment="Center" 
                          Margin="0,20">
                    <Button Content="保存" Width="100" Height="28"
                           Margin="0,0,10,0" Click="SaveButton_Click"/>
                    <Button Content="取消" Width="100" Height="28"
                           Click="CancelButton_Click"/>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>